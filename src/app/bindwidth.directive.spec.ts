import { BindwidthDirective } from './bindwidth.directive';

describe('BindwidthDirective', () => {
  it('should create an instance', () => {
    const directive = new BindwidthDirective();
    expect(directive).toBeTruthy();
  });
});
